[pytest]
markers =
    integration: marks tests as integration tests that hit a live server

# The repo .venv has CKAN installed which registers pytest plugins that require
# a CKAN_INI config file. Disable them when running integration tests:
#
#   pytest -p no:ckan -p no:ckan_fixtures tests/integration/ -v
